### 4.1 Connection handling, security and QoS

<BilingualSection>
  <template #ko>
<div>

MQTT 프로토콜은 클라이언트에 대해 last will 메시지를 설정하는 옵션을 제공합니다.
클라이언트가 어떤 이유로든 예기치 않게 연결을 끊으면, last will이 브로커에 의해 다른 구독 클라이언트에게 배포됩니다.
이 기능의 사용은 [6.5 Connection](/spec/6/6.5#65-connection) 절에 기술되어 있습니다.

모바일 로봇이 브로커와의 연결을 끊으면, 모든 Order 정보를 유지하고 마지막으로 해제된 노드까지 Order를 이행합니다.

통신 오버헤드를 줄이기 위해 `order`, `instantActions`, `state`, `factsheet`, `zoneSet`, `responses`, `visualization` topic에는 MQTT QoS 수준 0(Best Effort)을 사용해야 합니다. `connection` topic에는 QoS 수준 1(At Least Once)을 사용해야 합니다.

프로토콜 보안은 브로커 구성을 통해 고려해야 하며, 본 지침에서는 다루지 않습니다.

</div>
  </template>
  <template #en>
<div>

The MQTT protocol provides the option of setting a last will message for a client.
If the client disconnects unexpectedly for any reason, the last will is distributed by the broker to other subscribed clients.
The use of this feature is described in Section [6.5 Connection](/spec/6/6.5#65-connection).

If the mobile robot disconnects from the broker, it keeps all the order information and fulfills the order up to the last released node.

To reduce the communication overhead, the MQTT QoS level 0 (Best Effort) shall be used for the topics `order`, `instantActions`, `state`, `factsheet`, `zoneSet`, `responses` and `visualization`. QoS level 1 (At Least Once) shall be used for the topic `connection`.

Protocol security needs to be taken into account by broker configuration, but is not addressed within this guideline.

</div>
  </template>
</BilingualSection>
